# 3d66 标签系统｜仓库开发规则

本文件适用于整个仓库。任何新对话、Codex 实例或开发者开始工作前都必须先阅读本文件。

## 1. 开始工作前

1. 确认工作目录是 `D:\3d66-label-system`，不要在 OneDrive 副本中开发。
2. 依次阅读 `PRODUCT.md`、`PROJECT_STATUS.md`、`CODEX_HANDOFF.md` 和 `docs/decisions/README.md`。
3. 执行 `git status --short` 和 `git log --oneline -10`。
4. 工作树中的既有修改默认属于用户或其他进行中的工作；不得覆盖、回退或顺带提交。
5. 先复现并读取真实数据、原始模型响应或页面状态，再决定修改提示词、解析器还是评分规则。

仓库文档之间的优先顺序是：

```text
PRODUCT.md（稳定产品目标）
→ 本文件 AGENTS.md（强制开发规则）
→ docs/decisions/（已接受架构决策）
→ PROJECT_STATUS.md（当前进度与缺口）
→ CODEX_HANDOFF.md（历史背景与长交接）
```

用户当前对话中的明确要求始终优先；如果它改变了长期产品或架构，必须在同一次修改中更新相应文档和测试。

## 2. 不可破坏的产品约束

- `Asset` 与 `EvaluationResult` 是一对多；素材页面只展示素材，不得把某次评测结果重新塞回素材实体。
- 同一素材必须能被不同模型和不同 A/B 提示词版本重复评测。
- 审核详情以 `evaluation_id` 定位具体运行，不能只依赖素材 ID 或“最新结果”。
- 默认 A 阶段负责范围、分类、形态、拍摄方式和画质，B 阶段负责美感维度，`out_of_scope` 不调用 B；兼容单提示词一次返回完整预检与美感结构，但不得绕过服务端评分。
- 模型输出维度和证据；总分、等级及等级限制由服务端版本化评分规则计算。
- 提示词只能创建新版本；禁止覆盖已发布或已归档版本。
- 模型、A 提示词、B 提示词、Rubric、评分引擎和抽样策略必须可追溯。
- 更换模型后必须把模型×提示词视为新候选组合，并使用锁定黄金样本回归。
- 人工确认的真值高于任何单个模型答案；旧模型和 ChatGPT/SOL 都只能作为参考。
- AI 可以提出提示词草案，但不能自动覆盖或绕过人工发布门禁。
- 高风险和冷启动结果不能为了降低审核量而被普通随机抽样替代。

## 3. 数据与安全

- 不得在代码、测试、日志、截图、Markdown、Git 提交或回复中输出真实 API Key、Cookie、会话令牌或明文密码。
- `.env`、SQLite 数据库、图片、日志、`.venv`、`node_modules` 和构建产物不得提交到 Git。
- API Key 继续通过 Windows DPAPI 加密；换电脑后重新填写，不尝试迁移可解密密钥。
- 默认运行数据位于 `%LOCALAPPDATA%\3d66-label-system`。未经用户授权，不删除、搬移或批量改写这些数据。
- Demo 当前采用本地永久保存；对象存储、保留策略、素材库和外部系统同步不在当前范围。

## 4. 提示词、模型与评分修改

- Doubao Lite 优先使用短 A/B 提示词、严格 JSON 结构和服务端校验，不通过无限堆叠提示词修复问题。
- 修改输出字段时必须同步更新：提示词 Schema、后端解析/适配、前端类型、界面和测试。
- 修改维度、权重、等级锚点或限制规则时，必须提升 Rubric/Engine 版本并保留旧结果的解释能力。
- 处理“空字段、机械同分、画质误判、专业摄影误判、等级虚高”时，应保存回归样本并增加针对性测试。
- 发布流程目标是“候选组合 → 黄金回归 → 达标 → 人工发布”；不得把“回归任务已创建”等同于“发布门禁已通过”。

## 5. UI 约束

- 中文单语界面；全站使用微软雅黑。
- 白底/浅色界面，不提供暗色模式；审图画布必须为白色。
- 品牌色为 `#CCED46`，只用于行动、选择、进度和焦点。
- 按钮文字加粗；分割线浅；文件名使用灰色细字。
- 保持建筑刊物式、编辑感、克制和高信息密度。
- 禁止模板化后台、紫蓝渐变、玻璃拟态、霓虹光、重阴影和无意义卡片堆叠。
- 状态不能只靠颜色表达；至少提供文字或图标。保持键盘焦点和 200% 缩放可用。

## 6. 修改与验证规范

- 使用小而清晰的修改，避免在同一提交中混入无关重构。
- 修改本地文件优先使用补丁方式；不得使用破坏性 Git 命令。
- 后端行为变化必须有测试；修复线上样本问题时至少增加一个对应回归用例。
- 前后端契约变化必须同步更新 `frontend/src/lib/types.ts`。
- 前端页面变化必须完成 TypeScript/Vite 正式构建，并在真实浏览器中检查主要流程和控制台错误。
- 数据模型新增字段或表时，要验证旧 SQLite 数据库可以通过现有启动迁移方式安全升级。

标准验证命令：

```powershell
cd D:\3d66-label-system\backend
..\.venv\Scripts\python.exe -X utf8 -m pytest tests -q

cd D:\3d66-label-system\frontend
npm.cmd run build
```

## 7. 完成定义

一项开发只有同时满足以下条件才算完成：

1. 用户要求的真实流程可用，不只是页面占位。
2. 数据关系、版本快照和历史记录保持正确。
3. 相关测试通过，前端构建通过。
4. 真实浏览器完成关键路径验收。
5. `PROJECT_STATUS.md` 已更新；若产生长期决策，新增或更新 ADR。
6. 只提交本次相关文件，提交信息清晰。
7. 工作区中其他人的未提交修改被完整保留。

## 8. 结束工作时

- 更新 `PROJECT_STATUS.md` 的当前提交、验证结果、已完成事项、进行中事项和下一步。
- 架构取舍写入 `docs/decisions/`，不要只留在聊天记录。
- 检查 `git diff --check`、`git status --short` 和最近提交。
- 如果已配置远程仓库，在不包含秘密和无关修改的前提下推送；若无法推送，明确说明本地领先多少提交。
