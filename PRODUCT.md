# Product

## Product Positioning and Naming

统一产品名称为 **特鹏标签中台（Label System）**。它是“标签体系重构”的统一产品载体，
同时是标签/内容中台重构的通用底座；“标签体系重构”是转型目标，不再作为与标签中台并列的
独立项目。产品覆盖素材接入、类目评测与标注、人工纠偏、AI 机制迭代、模型与版本治理、
存量重跑、正式发布和下游消费，不定义为仅用于评测或提示词实验的工具。

后续 PRD、路线图和项目状态以特鹏标签中台为产品主体。业务类目作为场景扩展，只定义
类目特有的合同、提示词、维度、规则、回归门槛和交互，不复制模型管理、纠偏闭环、AI 迭代、
版本发布、存量重跑或下游消费等平台通用能力。

## Register

product

## Users

3D66 内容运营、素材审核、算法评测与平台集成人员。他们在办公室或局域网环境中处理上游增量素材、观察模型评测证据、完成少量必要纠偏与二审，并管理类目、模型和提示词版本。

## Product Purpose

将多类目内容的标签生产变成可追溯、可自动化治理的闭环：下游字段需求合同驱动素材接入，系统编排
标注路径与任务，模型和人工共同产出结果，人工完成纠偏验收，正式版本向下游发布并支持引用/对账，
Badcase 再回流为机制迭代输入。上游以增量事件接入素材，模型产出候选，人工只处理纠偏与二审，系统
自动沉淀黄金案例、回归和优化证据；下游只读取版本化的正式发布标签。默认采用 A/B 两阶段，也兼容
一次返回完整结构的单提示词模式。成功意味着审核员能快速判断模型是否可信，模型升级后能用相同样本和
评分规则比较效果，并保留每次运行的模型、提示词、规则、类目合同和评分引擎版本。

## Data and Projection Boundaries

`semantic.*`、`quality.*`、`governance.*` 等资产字段属于中台 Canonical 事实；人工真值、证据、来源、
模型/规则版本、审核与发布状态必须可追溯。搜索索引、知识图谱和向量索引只是可重建消费投影，不能成为
事实主库。TPENG 中台后续将以“资产语义投影服务”承载图片、文本片段和多模态 Embedding 的版本与
生命周期；Query×素材相关性、排序权重、召回融合、在线实验及知识图谱内部关系属于下游策略，不进入
素材事实。具体边界和当前 Gap 见 ADR-0043。

下游数据库消费默认采用 **一个统一大维表 + 数个职责明确的小表**：大维表承载大多数正式标签与
质量字段的稳定读取，小表承载搜索标签、质量过滤、治理来源、变更事件或类目专用字段。所有表都只投影
正式发布事实，必须携带资产、标签、机制和模型版本并支持幂等重建、哈希对账与失败补偿；任何下游表都
不能反向成为 Canonical 事实主库。

## Product Shell and Workspaces

产品前端以“增量评测”和“存量回归”两个串行工作区承载不同任务上下文，二者共享素材、模型、机制、
纠偏、AI 迭代、质量、发布、投影与运行调度平台内核。复杂配置、历史、指标和原始证据下沉到抽屉、
Dialog 或专用工作台；一级页面只保留操作主线所需状态、决策证据和动作。类目差异通过受控 profile
插件扩展，未知或未启用 profile 只读诊断且禁止创建候选或启动执行。

## Brand Personality

精确、编辑感、克制。界面像建筑设计刊物与专业图像检测台的结合，具有明确的品牌识别，但不干扰长时间审核。

## Anti-references

不做传统模板化后台，不使用暗色审图画布，不使用玻璃拟态、紫蓝渐变、霓虹光晕、胶囊堆叠、重阴影和重复卡片网格。避免为了“AI感”牺牲信息密度与可读性。

## Design Principles

- 证据先于结论：评分必须紧邻可定位的视觉证据。
- 版本就是数据：模型、提示词、规则和评分引擎版本始终可追溯。
- 审核路径最短：批量操作、异常复核和键盘操作优先。
- 自动化优先：常规增量、组批、回归和候选生成由系统推进；人工只在纠偏、二审与例外处决策。
- 发布层隔离：候选、人工确认与正式发布标签分层；前端消费只能读取已发布版本和变更流。
- 强调色有含义：酸性青柠只表示行动、选择、进度和焦点。
- 熟悉但有辨识度：标准控件与清晰状态之上，用编辑排版建立品牌感。

## Accessibility & Inclusion

以 WCAG 2.2 AA 为基线，支持键盘导航、清晰焦点、200% 缩放、减弱动效和不依赖单一颜色的状态表达。正文与操作控件保持高对比，小字号控件不使用衬线字体。
