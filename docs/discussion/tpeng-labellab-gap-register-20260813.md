# TPENG 标签实验台下一阶段 Gap 清单（2026-08-13）

## 用途

本清单是当前冻结批次完成后返回【标签体系】重构会话的范围冻结输入，不是实施授权。Owner 需要逐项冻结
目标、范围、权限、数据源、回退、验收和停止条件；冻结后仍由 TPENG 标签实验台会话作为唯一代码写入方。

## 当前已经具备

| 能力 | 当前证据 | 边界 |
|---|---|---|
| 双工作区 | 增量评测、存量回归、运行中心、质量资产一级入口与串行步骤 | 页面隔离、平台核复用；桌面端验收 |
| 本地增量接入 | `content-ingress-v1` 按类目/profile 幂等创建 `production_import` 素材包 | 不连接真实上游，不自动创建评测 Job |
| 运行承载证据 | 五类队列、并发配额、重试延迟、熔断、恢复队列、检查点 | 现有单机调度控制面，不宣称生产级多实例 |
| 黄金数据集 | 普通/黄金/挑战集、真值修订、锁定、CSV/JSON/Manifest 导出 | 锁定版本不可原地覆盖 |
| 平台字段需求合同 | 九个通用语义字段加本地化 title、类目适用性、执行变体、质量门槛、版本与显式激活 API | 当前仅本地合同与 fixture；种子默认 draft，不自动激活 |
| 语义事实治理 | AssetVersion、证据候选、精确/别名实体映射、人工批准、追加式 SemanticTagFact、published-label-v2 | 只有 approved 事实进入正式发布；v1 历史继续可读 |
| 质量指标 | 原有 L1-L5 矩阵/三档指标与字段级语义 Precision、Recall、映射、冲突、纠偏、审核、对账指标并存 | 指标只作人工决策证据，不自动启用候选 |
| 类目扩展 | 标准图像、Proposal PDF 可执行；3D/SU 已有专用机制并完成 whole/single 本地语义纵向 fixture | 3D/SU 不是通用语义字段专属实现；未知 profile fail-closed |
| 多表投影合同 | 一个统一大维表和数个小表的 Registry、Manifest、本地适配器、semantic/quality/governance 变换和对账 | 只消费正式事实，不写真实业务库 |

## 下一阶段候选 Gap

| Gap | Owner 冻结输入 | 建议验收证据 | 当前禁止事项 |
|---|---|---|---|
| 真实字段合同签认 | 知识图谱/搜索/大维表/小表逐字段 Owner、标准词表、权重含义、空值语义、SLA 与变更审批 | 已有本地合同升级为签认版本，兼容性检查和字段级对账通过 | 不能把本地 fixture 或 3D/SU 默认词表当成全平台正式标准 |
| 标准词表与实体映射治理 | 中英文实体 ID、别名审批、版本归属、冲突处理、退役和回退策略 | mapping version、未映射队列、双语一致率和回退演练 | 不做无约束 fuzzy 映射，不让知识图谱反向成为事实主库 |
| 真实上游接入 | 来源系统、鉴权、事件重放、资产拉取、乱序/删除语义、限流 | 金丝雀事件、幂等/冲突/补偿、断网恢复 | 不复制凭据，不直接镜像上游数据库 |
| 真实数据库投影 | 目标库表、最小权限、单写者、事务/批次、幂等键、回滚 | 快照、金丝雀、行数/缺失/哈希/版本对账、回滚演练 | 未审批不得执行 DML |
| 跨站点来源身份绑定 | 国内 `dim_res_info_union.ll_id` 与海外 `ods_ll_relebook_res.res_id` 的双身份合同；海外 `su_extra.is_single` 关联键、快照和匹配唯一性 | 两套来源分别完成主键/空值/重复/`is_single`/跨表覆盖探查，独立 `probe_hash`，再绑定到四批路由 | 当前运行时仍以单一 `source_identity` 和国内 `(res_type,ll_id)` 为主；未签认海外关联键前不接真实来源 |
| 生产级投影 Worker | 分片、检查点、重试、补偿、熔断、多实例租约和观察窗口 | 故障注入、断点恢复、重复投递、漂移告警 | 不能反向改写 Canonical 事实 |
| 大规模存量重跑 | 范围选择、预算、并发、快照、差异审核、标签发布申请 | 1 万级压测、断点恢复、差异包和人工发布回执 | 机制启用不得自动覆盖存量 |
| 3D/SU 真实生产切片 | 正式素材来源、整体/单体识别、签认词表、黄金集、真实模型、生产门槛和消费表 | 国内 whole/single 金丝雀、字段质量门禁、发布/投影/Badcase 回流回执 | 不用本地 fixture 替代真实数据，不复制平台通用能力 |
| 海外与其他类目扩展 | 海外 locale/词表/模型/Prompt 版本及其他类目字段适用性矩阵 | 同一合同参数路由、双语一致率、类目隔离回归 | 不复制国内/海外或各类目流水线 |
| Embedding 生命周期 | 资产/片段身份、模型版本、生成、失效、重算、回退、质量对账 | 资产/模型版本重建和漂移验证 | 不把向量定义为标签 |
| 首个真实消费切片 | 优先 3D 搜索的字段需求、评测集、流量和效果指标 | 标签生产到 Badcase 回流的端到端回执 | Query×素材相关性和排序权重不进入素材事实 |

## 90 天证据链建议

1. 正式标签生产：真实字段需求合同驱动一个类目增量素材完成评测与人工纠偏。
2. 双轴发布：候选机制人工启用，正式标签事实另行人工发布，全程可追溯。
3. 数据库投影：正式事实生成统一大维表与目标小表，完成版本、行数、缺失和哈希对账。
4. 首个消费场景：优先选择 3D 搜索，只消费正式投影，不读取候选和人工过程。
5. 效果评估：以字段准确率、召回率为底座指标，结合搜索会话有效浏览等下游指标验证影响。
6. Badcase 回流：下游错例回到纠偏样本，触发 AI 候选与自动回归，最终仍由人工决定是否启用。

## 停止条件

- 发现真实数据库或外部系统被未授权写入；
- 候选机制、实验结果或人工过程被下游读取；
- 投影反向覆盖 Canonical 事实；
- 候选被自动启用、标签事实被自动发布或机制启用自动覆盖存量；
- 无法证明资产、标签、机制、模型版本与投影行之间的对账关系。
