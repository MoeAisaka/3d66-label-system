# 全局自动组批本地验收回执

日期：2026-08-18

## 已验证

- Task 4 严格类目、增量/存量、代次、机制指纹与路由隔离组批已存在并通过回归。
- 运营纠偏理由/证据路由：调用甲、调用乙、第三版规则，支持多节点按 A→B→V3 依赖顺序合并。
- 候选包使用规范化清单和 SHA-256 键，缺少真实机制指纹时不生成可消费包。
- 全局自动化接口：总览、泳道、批次、历史审计、候选二审；人工采用/拒绝不发布标签事实、不触发存量重跑。
- 灵感图五档指标与推荐档/常规档/过滤档三档兜底口径，推荐档占比 35% 门槛，质量准召 80% 门槛。
- 前端全局自动组批总览合同脚本、TypeScript 检查和生产构建通过。

## 测试证据

- 后端全量：`1561 passed, 1 skipped, 6 warnings`。
- 本轮新增/受影响专项：路由、候选包、接口、灵感图指标共 14 项通过；Task 4/纠偏/配对回归专项共 74 项通过。
- 前端：`npm run lint`、`npm run build`、`node --experimental-strip-types scripts/check-global-automation-contract.ts` 通过。
- 警告仅为既有 FastAPI/httpx 与 PDF SWIG 弃用提示；未发现业务失败。

## 发布前门禁

- 自动发布关闭；人工采用仍是必要门。
- 历史积压保持 `historical_audit`，不得自动消费。
- 真实调优模型配置、密钥、输入上限和计价必须在测试服页面完整配置；缺失时 fail-closed。
- 全局日预算按全部活动泳道共享 50,000,000 微元（50 元）。
- 生产环境不在本次范围。
