# 全局自动组批 Task 4 本地验证回执

日期：2026-08-17  
分支：`codex/global-auto-batching-mechanism-20260817`  
工作树：`/Volumes/WorkSSD/Codex/2026-08-11/labellab/work/global-auto-batching-mechanism-20260817`

## 交付范围

本批完成全局自动组批 Task 4 的消费者接入与回归覆盖：

- 数据库不存在任何 `enabled` 泳道时，保留旧消费者的类目/提示词组批语义。
- 数据库存在任一 `enabled` 泳道时，消费者切换为严格泳道模式，按类目、增量/存量链路、代次、机制指纹、路由和提示词版本隔离。
- 选择层排除 `historical_audit`、`awaiting_evidence`、`rejected` 和不完整泳道身份案例；阻塞原因继续以结构化代码返回。
- 选中的案例在租约领取前创建不可变 `AutomationBatch`，冻结批次策略、案例资格快照、案例集合哈希和泳道身份。
- `AutomationOptimizationRun` 的冻结输入、运行键、审计事件和返回值携带 `batch_id` 与 `lane_key`。
- dry-run 批次落账为 `completed`；真实执行沿 `processing → completed/failed` 更新；技术失败可沿同一冻结批次重试并生成新的运行记录，不重复生成批次。
- 旧消费者的预算、租约、重试、模型调用和人工发布门禁语义保持不变；候选不会自动采用、发布标签或覆盖存量。

## 验证证据

- 组批与消费者专项：`58 passed, 1 warning`。
- 后端全量：`1547 passed, 1 skipped, 6 warnings`。
- `python -m compileall -q backend/app`：通过。
- `git diff --check`：通过。
- 全量首跑使用 `PYTHONPATH=.` 在 `backend/` 下执行时仅因仓库根目录 `scripts` 不在导入路径而在收集阶段失败；改为计划要求的 `PYTHONPATH=..` 后全量通过。该问题是验证命令路径问题，不是业务或代码失败。

## 关键回归场景

- 同一类目不同路由一次只选择一条泳道，下一次调用按确定性顺序处理另一条泳道。
- 增量/存量、代次、机制指纹、路由和提示词任一不同均不会混批。
- 历史审计与待补证据案例不会被消费者调度。
- 批次策略和案例集合幂等；相同集合不会新增第二个批次。
- 首次技术超时后批次保持同一 `batch_id`，重试只新增运行记录并最终完成批次。

## 发布边界

本批只在隔离工作树完成代码、测试和文档提交：未读取或保存密钥，未调用真实模型，未启用真实自动化，未连接真实业务数据库，未推送、未创建或合并 Codeup MR，未部署测试服或生产环境。

后续仍待单独授权的范围：类目路由到调用甲/调用乙/第三版规则的候选生成、三角色回归实际执行、全局自动化与类目泳道页面、真实模型金丝雀和唯一合流部署。
